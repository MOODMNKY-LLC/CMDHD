# Root shared environment (copy to .env for local dev)
NODE_ENV=development
APP_ENV=local
NEXT_PUBLIC_SITE_NAME=CMDHD

# Add service keys below as needed later (Supabase, etc.)
# SUPABASE_URL=
# SUPABASE_ANON_KEY=
